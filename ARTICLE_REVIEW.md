# 292 State 文章对照分析

日期：2026-09-20。审查基线：8a168ed。以下问题记录对应升级前基线；后续实施结果见文末。

## 来源与证据边界

- 用户指定文章：https://blog.caowo.de/posts/chatgpt-codex-292-state-anti-degradation-2026/
- 官方客户端：https://github.com/openai/codex/blob/main/codex-rs/core/src/client.rs
- 官方 SSE 解析：https://github.com/openai/codex/blob/main/codex-rs/codex-api/src/sse/responses.rs

文章可作为实验线索，不能代替协议说明或受控实验。它将 292 描述为 HTTP 状态码，并将 312 定义为降智/撤销信号；本项目的 292/312 是 state 字符串长度，采集成功判定为 HTTP 200。这些概念不能直接合并。

官方 ModelClientSession 注释说明：x-codex-turn-state 用于同一轮的粘性路由，同轮保持不变，不应跨轮发送；跨轮复用会违反客户端/服务端约定并可能导致路由问题。因此不建议仅凭文章加强跨会话复用或在进行中的同一轮强制换票。

约一小时 TTL、长度与质量的对应关系，没有在本次查阅的官方实现中获得支持；这里的一小时是本项目的本地策略，不能作为服务端有效性保证。parseState 只进行结构和时间检查，未验证签名。

## 优先级与建议

### P0：明确 state 的作用范围

当前 entryKey 只有账号和配置模型，finishRequest 会覆盖客户端已有 state。建议默认保留客户端提供的 state，按上游来源、认证主体、完整 thread/turn 标识区分路由状态；身份或轮次不明确时只观察。同轮状态保持稳定，跨轮实验应独立开关且标明未验证。

### P0：补齐账号与模型识别

accountKey 缺失 chatgpt-account-id 时固定使用 default，不同认证请求可能共用缓存。handlesModel 在没有模型头时返回 true，未读取请求体实际模型。建议身份不明时停止缓存复用；如需认证指纹，使用可靠摘要且不记录原始凭据。缓存加入上游来源，模型信息明确且一致后才处理。请求体读取需先验证运行时对压缩正文的支持。

### P1：修复异常状态机与文档不一致

handleResponse 仅累加 strikes，shouldRenew 不读取 strikes。已通过真实入口的 VM 模拟确认：连续两次返回 312 后 strikes=2，shouldRenew 仍为 false。README 声称两次异常后强制采集，但当前未实现。

建议将观察值与决策分开：记录 state_changed、unexpected_shape、http_error，不能把所有异常都当成撤销。若保留实验模式，异常达到阈值后可标记下一轮重新获取，并尊重限流冷却；不要自动重放正式生成请求，也不要把 312 定义为已证实的降智。

### P1：优化续期和冷却

当前按签发时间第 50 分钟进入续期窗口，且仅在新请求到达时执行。默认失败冷却 5 分钟，401/403 冷却一小时；Retry-After 仅支持数字。正式响应中的 429 当前只通知，不更新探针冷却。

建议统一正式请求与探针的限流信息；支持日期格式的 Retry-After；普通临时失败采用有上限的退避及少量抖动，身份更新后重新评估认证冷却。说明中应准确写“请求触发续期”。独立后台 keeper 属于架构扩展，需要先验证设备运行条件，不能靠缩短 cooldown 实现。

### P1：加强并发与版本检查

probeUntil 通过持久化存储读改写，不能由此证明并发互斥；多个响应及探针可能覆盖彼此的状态。建议增加 generation/探针 ID，写入前比较版本和签发时间，拒绝迟到旧结果覆盖更新状态；若运行时没有原子能力，应明确剩余限制。

### P2：加强解析与诊断

streamCompleted 使用正则寻找完成标记，不是真正 SSE 事件解析，正文中的相似文本也可能匹配。建议逐事件解析并验证完成类型、失败事件以及 response 标识。正式响应目前仅观察响应头，不能宣称已确认完整生成成功。

当前 acceptState 验证块数而非严格字符长度：相同 10 块数据可采用带填充 292 字符或无填充 290 字符编码。VM 复现确认无填充 290 字符仍会接受。建议明确“292”为历史名称，并分别记录实际长度、块数和校验原因；不要用长度直接表达质量或授权状态。

保留用户要求的每次“292 打票成功”通知，同时在正文注明本地注入完成。补充结构化原因、最近续期时间、下次允许重试时间和同账号统计；不记录完整 state、凭据或正文。

## 已执行验证

未发真实探针，未使用账号凭据。Node VM 隔离运行真实脚本入口验证：

- 连续两个 312 响应：strikes=2，但 renewTriggered=false。
- 缺失模型头：unknownModelAccepted=true。
- 不同 Authorization 且缺失账号 ID：生成相同缓存键。
- 10 块、无填充的 290 字符 state：当前 acceptState 接受。

以上验证是实现行为证据，不是“降智”效果测试。真实收益需要在相同账号、模型和固定任务下比较成功率、错误率、延迟、任务通过率及额外探针开销。

## 后续实施结果

已按用户要求完善逻辑：默认同轮复用、保留客户端 state、SHA-256 认证摘要和完整轮次隔离、显式实验模式、严格 SSE 解析、统一限流与退避、异常安排后续新轮采集、探针及候选票独立代次、票指纹核对和迟到回调保护。读取异常或损坏存储直接透传，不写空数据；写入失败不注入。

实验模式保持用户要求的每次注入通知，续期仅由新轮次请求触发；探针成功也有最小冷却。34 组真实入口模拟测试通过。新模块使用 v2 缓存且默认 mode=turn；旧模式必须显式设置 mode=experimental。没有后台采集、严格原子锁或真实设备验证。
