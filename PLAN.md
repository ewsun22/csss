# 计划

## 当前需求：Shadowrocket 提示栏通知

- [x] 收到有效 state 后继续沿用现有校验和缓存流程，采集成功通知显示实际有效期。
- [x] 每次使用缓存注入时发送“292 打票成功”通知，正文说明注入的 state 长度。
- [x] 探针及正式响应收到 312 时提示“收到 312，请注意”；补充其他异常 state、401/403、429、网络和探针失败提醒。
- [x] 无可用缓存时提示本次未注入；通知不可用或抛错不影响请求。
- [x] 更新说明，通过模拟 Shadowrocket 运行时验证通知、请求透传及缓存行为。

验证结果：`node test-codex-state-shadowrocket.js` 和 `node --check codex-state-shadowrocket.js` 均通过。尚未在真实 Shadowrocket 设备上验证系统横幅。

## 提交与推送

- [x] 检查变更范围并重新运行本地测试。
- [x] 将通知功能、说明和测试提交至 main 并推送 origin（功能提交：32042d0）。

## 文章对照分析（2026-09-20）

- [x] 阅读用户指定文章，对照官方 Codex 源码确认 state 的使用范围。
- [x] 审查当前采集、续期、隔离、通知及异常处理实现。
- [x] 通过隔离运行时复现连续 312 不触发续期、未知模型放行、缺失账号 ID 共用缓存问题。
- [x] 将证据、优先级和建议记录到 ARTICLE_REVIEW.md；本轮仅分析，不修改运行逻辑。
- 后续实施情况：见下方完善逻辑记录及 ARTICLE_REVIEW.md 的实施结果。

## 完善逻辑并提交推送（2026-09-20）

- [x] 默认同轮复用：保留客户端 state，按来源、认证摘要、账号、模型及完整 thread/turn 隔离；未知身份/模型/关联 ID 时透传。
- [x] 跨轮复用仅在 mode=experimental 时启用；同轮锁定状态，异常仅安排后续新轮次重新采集。
- [x] 统一探针和正式响应的认证/限流冷却，解析 Retry-After 秒数与日期，普通失败使用有上限的退避。
- [x] 增加探针 ID、代次和新旧票比较，迟到结果不能覆盖更新状态；持久化失败时透传。
- [x] 严格解析 SSE 完成事件和 response ID，区分长度、结构、HTTP 状态及失败原因。
- [x] 更新模块、迁移说明、通知及诊断；增加真实入口模拟测试覆盖隔离、续期、并发和失败场景。
- [x] 完成 34 组测试、语法检查与 diff 审查。
- 提交发布步骤：提交并推送 origin/main，随后核验远端与工作区；执行结果以本轮最终回复为准。

边界：不增加后台守护进程，不改变节点或重放正式请求；未在真实设备验证的行为写明限制。

验证：`node --check codex-state-shadowrocket.js`、`node test-codex-state-shadowrocket.js`（34 组）、`git diff --check` 通过。独立代码审查指出的存储读取失败、代次分离、成功冷却及候选票指纹问题均已修复并增加回归测试。
